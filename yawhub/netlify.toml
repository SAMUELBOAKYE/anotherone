[build]
  base = "yawhub"
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://anotherone-2.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200